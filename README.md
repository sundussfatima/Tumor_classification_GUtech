# Brain Tumor Classification (Lightweight CNN)

This project implements a lightweight Convolutional Neural Network (CNN) for brain tumor classification using TensorFlow/Keras.

## Project Structure
